<%= form_with(model: livin_room, local: false) do |form| %>
  <% if livin_room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(livin_room.errors.count, "error") %> prohibited this livin_room from being saved:</h2>

      <ul>
      <% livin_room.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
